pipeline {
    agent any

    stages {
        stage('Test OpenCart') {
            agent {
                docker {
                    image 'maven:3.9.9-eclipse-temurin-17' // Java + Maven
                    reuseNode true
                }
            }
            steps {
                sh '''
                    mvn clean test -DsuiteXmlFile=master.xml
                '''
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                    archiveArtifacts artifacts: 'target/surefire-reports/*', followSymlinks: false
                }
            }
        }
    }
}
